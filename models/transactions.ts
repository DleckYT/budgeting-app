export interface Transaction {
    id: number
    created_at: string
    name: string
    amount: number
    category_id: number
  }
  
  export interface TransactionData {
    created_at: string
    name: string
    amount: number
    category_id: number
  }
  